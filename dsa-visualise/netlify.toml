[build]
  base = "dsa-visualise"
  publish = "dist"
  command = "npm install --legacy-peer-deps && npm run build"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
